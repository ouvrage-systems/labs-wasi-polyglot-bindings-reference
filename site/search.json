{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"WASI Polyglot Bindings Reference Lab","text":"<p>Welcome to the official documentation for the WASI Polyglot Bindings Reference Lab (an Ouvrage Systems initiative). </p> <p>This repository serves as a hands-on reference laboratory exploring WebAssembly (WASM) and the WebAssembly System Interface (WASI). It demonstrates how to compile Go libraries into WebAssembly modules and execute them natively in-process inside host scripting environments like Python and JavaScript (Node.js &amp; Web Browsers).</p>","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"#table-of-contents","level":2,"title":"📖 Table of Contents","text":"","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"#section-1-foundations-architecture","level":3,"title":"📂 Section 1: Foundations &amp; Architecture","text":"<ul> <li>WASM &amp; WASI: The Big Picture<ul> <li>Learn the historical context: Web-only WASM Core (2017) \\(\\rightarrow\\) WASI Preview 1 Server POSIX (2019) \\(\\rightarrow\\) WASI Preview 2 Component Model (2024+).</li> </ul> </li> <li>The IPC Dilemma: Stdio vs. In-Process<ul> <li>Understand the performance and architectural trade-offs between JSON-RPC subprocess piping (V1) and memory-mapped native function exports (V2).</li> </ul> </li> </ul>","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"#section-2-laboratory-implementation","level":3,"title":"📂 Section 2: Laboratory Implementation","text":"<ul> <li>Go Core &amp; The WIT Contract<ul> <li>Examine the internal Go library structure (<code>pkg/geometry</code>, <code>pkg/store</code>) and the WIT (WebAssembly Interface Types) contract declaring our methods and stateful resources.</li> </ul> </li> <li>Toolchain &amp; Compilation Workflow<ul> <li>A step-by-step guide to compiling targets using TinyGo, <code>wasm-tools</code>, <code>wit-bindgen</code>, and the reactor adapter.</li> </ul> </li> </ul>","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"#section-3-host-integrations","level":3,"title":"📂 Section 3: Host Integrations","text":"<ul> <li>Python Host Integration<ul> <li>How to load WASI modules under CPython, execute pytest suites, and ensure static type safety.</li> </ul> </li> <li>JavaScript Host Integration &amp; Bundlers<ul> <li>Running unit tests under Node.js, and configuring modern bundlers (Vite &amp; Webpack 5) with fallbacks for browser-compatible shims.</li> </ul> </li> <li>WASI Networking &amp; Sockets<ul> <li>Capabilities-based networking, opening raw TCP sockets, and executing outbound HTTP requests in Go.</li> </ul> </li> </ul>","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"#section-4-playground-troubleshooting","level":3,"title":"📂 Section 4: Playground &amp; Troubleshooting","text":"<ul> <li>Live WASI V2 Browser Demo<ul> <li>The Masterstroke: Run our actual compiled Go V2 WASI component live inside your browser, doing real-time OOP geometry calculations and interacting with a stateful KV-Store.</li> </ul> </li> <li>Developer Tips &amp; Troubleshooting<ul> <li>Tricks of the trade: bypass browser MIME-type constraints locally, mock system hooks for standalone runtimes, and inspect memory heaps.</li> </ul> </li> </ul>","path":["WASI Polyglot Bindings Reference Lab"],"tags":[]},{"location":"foundations/","level":1,"title":"WASM &amp; WASI: The Big Picture","text":"<p>WebAssembly (WASM) has evolved from a browser-only speed booster to a universal, language-agnostic code execution format. To understand the architecture of this laboratory, it is essential to trace the historical timeline and the core motivations that drove the WebAssembly ecosystem to where it is today.</p>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#1-webassembly-core-2017-speeding-up-the-browser","level":2,"title":"1. WebAssembly Core (2017) — Speeding Up the Browser","text":"","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-motivation","level":3,"title":"The Motivation","text":"<p>In 2017, JavaScript was the only language capable of executing code inside web browsers. However, JS is a dynamic, interpreted language. Even with JIT (Just-In-Time) compilation, JS is poorly suited for compute-heavy tasks like 3D gaming, video editing, cryptography, or CAD software.</p> <p>The goal of the WebAssembly Core MVP (Minimum Viable Product) was to bring near-native execution speeds (C/C++, Rust, Go) to the browser under a secure, standard runtime.</p>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-architecture","level":3,"title":"The Architecture","text":"<ul> <li>Minimal Virtual Machine: WebAssembly is a low-level, binary instruction format for a stack-based virtual machine. It operates in a completely isolated, linear memory space (a simple contiguous array of bytes).</li> <li>The Sandbox: By default, WASM has zero access to the host machine. It cannot interact with the DOM, make network requests, read files, or check the system clock.</li> <li>Imports/Exports: The only way for WASM to perform actions is through an explicit handshake. The host (the browser's JS engine) imports functions into the WASM module (e.g., a function to write to a Canvas) and calls exported functions from the module (e.g., <code>Add(a, b)</code>).</li> </ul>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-limitation","level":3,"title":"The Limitation","text":"<p>WASM Core is stateless and silent. It does not know what an Operating System is.</p>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#2-wasi-preview-1-2019-reaching-the-server","level":2,"title":"2. WASI Preview 1 (2019) — Reaching the Server","text":"","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-motivation_1","level":3,"title":"The Motivation","text":"<p>Seeing the lightweight nature, rapid startup times, and solid sandboxing of WASM, server-side developers realized it could be an excellent alternative to heavy Docker containers for serverless functions, plugins, and edge computing.</p> <p>However, server-side code must interact with the Operating System: it needs to print logs to the terminal, read config files, and listen on ports. Thus, the Bytecode Alliance created WASI (WebAssembly System Interface) to provide a standardized API between WASM and the host OS.</p>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-posix-architecture","level":3,"title":"The POSIX Architecture","text":"<ul> <li>WASI Preview 1 (<code>wasip1</code>) chose to emulate Unix/POSIX system calls.</li> <li>It exposes standard imports like <code>fd_read</code> (read from a file or stdin), <code>fd_write</code> (write to stdout/console), and <code>path_open</code>.</li> <li>A Go or Rust program compiled to <code>wasip1</code> behaves exactly like a native command-line binary running on Linux.</li> </ul>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-limitations-of-preview-1","level":3,"title":"The Limitations of Preview 1","text":"<ol> <li>Improper for Browsers: The browser is not a Unix operating system. To run a <code>wasip1</code> binary in Chrome, the JavaScript host must simulate a Unix OS in memory (mapping file descriptors, intercepting stdin/stdout streams).</li> <li>No High-Level Types (The Byte Nightmare): POSIX only understands raw bytes. If you want to call a function with complex parameters (like passing a struct or returning a string), you must manually encode/decode the structure into WASM's linear memory. This is highly tedious, error-prone, and compromises memory safety.</li> </ol>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#3-wasi-preview-2-the-component-model-2024-unification","level":2,"title":"3. WASI Preview 2 &amp; The Component Model (2024+) — Unification","text":"","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-motivation_2","level":3,"title":"The Motivation","text":"<p>To build modular systems, developers need software components that can talk to each other using clean, high-level interfaces, rather than piping raw bytes over virtual Unix file descriptors. WASI Preview 2 (<code>wasip2</code>) solves this by moving away from POSIX emulation toward the Component Model.</p>","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"foundations/#the-architecture_1","level":3,"title":"The Architecture","text":"<ul> <li>WIT (WebAssembly Interface Types): Instead of calling low-level system imports, components declare their inputs and outputs using a contract file (<code>.wit</code>).</li> <li>High-Level Types: WIT natively supports structures (<code>record</code>), choices (<code>variant</code>), lists, options, and stateful objects (<code>resource</code> with constructors and methods).</li> <li>Universal Binding Generation: Toolchains like <code>wit-bindgen</code> and <code>jco</code> parse the WIT file and automatically generate clean bindings for the host. Mappings, memory allocations, and pointer manipulations are handled automatically.</li> <li>Decoupled Capabilities: A component does not request \"a file descriptor\". It requests an interface (e.g., <code>import wasi:clocks</code>). <ul> <li>On the server, <code>wasmtime</code> routes this to the system clock.</li> <li>In the browser, JS routes this to <code>performance.now()</code>.</li> <li>No Unix emulation is required.</li> </ul> </li> </ul> Feature WASM Core (2017) WASI Preview 1 (2019) WASI Preview 2 (2024+) Primary Environment Web Browsers Server-Side Runtimes Universal (Web, Server, Edge) System Abstraction None POSIX (Unix System Calls) Interface Types (WIT Contract) Data Types Integers / Floats only Raw Bytes (Pointers) Records, Variants, Lists, Resources Browser Compatibility Native Via Unix Emulation shims Native (via JS bindings &amp; polyfills) Composition Hand-written JS glue Unix Pipe redirection Native Component Linking","path":["WASM &amp; WASI: The Big Picture"],"tags":[]},{"location":"go-and-wit/","level":1,"title":"Go Core &amp; The WIT Contract","text":"<p>This page explores the core Go implementation of our library and the WIT (WebAssembly Interface Types) contract that defines our module boundaries in WASI Preview 2.</p>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#1-core-go-library-layout","level":2,"title":"1. Core Go Library Layout","text":"<p>To demonstrate clean decoupling, the business logic of our library is written in pure Go packages inside the <code>pkg/</code> directory. It has no knowledge of WebAssembly or WASI:</p> <ul> <li><code>pkg/geometry/geometry.go</code>: Contains structs (<code>Point</code>, <code>Rectangle</code>, <code>Circle</code>, <code>Triangle</code>) and calculation logic.</li> <li><code>pkg/store/store.go</code>: Implements a thread-safe in-memory key-value database.</li> <li><code>pkg/text/text.go</code>: Handles basic text formatting and string manipulation.</li> </ul>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#2-the-wit-contract-worldwit","level":2,"title":"2. The WIT Contract (<code>world.wit</code>)","text":"<p>The WIT contract (located at <code>bindings/wit/world.wit</code>) is the single source of truth for the V2 component interface. It defines our types and exports:</p> <pre><code>package ouvrage-lab:wasi-demo;\n\nworld wasi-polyglot-reference {\n    // 1. Geometry package exports\n    import wasi:clocks/wall-clock@0.2.0;\n\n    export geometry;\n    export lang;\n    export store;\n}\n\ninterface geometry {\n    record point {\n        x: float64,\n        y: float64,\n    }\n\n    record rectangle {\n        top-left: point,\n        bottom-right: point,\n    }\n\n    distance: func(p1: point, p2: point) -&gt; float64;\n    area-rectangle: func(r: rectangle) -&gt; float64;\n    ...\n}\n\ninterface store {\n    resource kv-store {\n        constructor();\n        set: func(key: string, value: string);\n        get: func(key: string) -&gt; option&lt;string&gt;;\n        delete: func(key: string) -&gt; bool;\n    }\n}\n</code></pre>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#3-values-vs-resources-the-architectural-distinction","level":2,"title":"3. Values vs. Resources: The Architectural Distinction","text":"<p>A critical concept in WASI V2 is the difference between passing values (<code>record</code>) and passing resource handles (<code>resource</code>):</p>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#a-pass-by-value-record","level":3,"title":"A. Pass-by-Value (<code>record</code>)","text":"<p>In the <code>geometry</code> interface, <code>point</code> and <code>rectangle</code> are defined as <code>record</code> types.</p> <ul> <li>What it is: A record is a plain data structure (similar to a struct in Go or an object literal in JS).</li> <li>How it behaves: When calling <code>distance(p1, p2)</code>, the host copies the field values (<code>x</code> and <code>y</code>) directly into the WASM memory heap. The WASM component processes the calculations and returns the result.</li> <li>No State: Once the function returns, the memory occupied by the record is garbage-collected. The record has no identity or handle inside WASM.</li> </ul>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#b-pass-by-reference-resource","level":3,"title":"B. Pass-by-Reference (<code>resource</code>)","text":"<p>In the <code>store</code> interface, <code>kv-store</code> is defined as a <code>resource</code>.</p> <ul> <li>What it is: A resource is a stateful object that lives inside the WASM component's heap memory.</li> <li>How it behaves: When the host calls <code>new KVStore()</code>, the Go WASM runtime creates an instance of the database, stores it in an internal Go map, and returns an integer index wrapper called a resource handle.</li> <li>The Handle: The host JS or Python wrapper only holds this numeric handle (e.g., <code>0</code>, <code>1</code>). When calling <code>db.set(\"username\", \"gpineda\")</code>, the host passes the handle along with the arguments. Go retrieves the database instance matching the handle and writes the state.</li> <li>Persistent State: The database persists in Go's memory between function calls. When the host garbage-collects the JS/Python wrapper, the host runtime calls the resource's destructor, cleaning up the database in Go's memory.</li> </ul>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#4-go-binding-implementation-bindingswasmv2","level":2,"title":"4. Go Binding Implementation (<code>bindings/wasm/v2/</code>)","text":"<p>When compiling to a V2 Component, <code>wit-bindgen-go</code> generates the low-level mapping code inside <code>bindings/wasm/gen/</code>. To keep the code clean and maintainable, the Go target package is split into dedicated files inside <code>bindings/wasm/v2/</code>:</p>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#a-the-registry-entrypoint-v2maingo","level":3,"title":"A. The Registry Entrypoint (<code>v2/main.go</code>)","text":"<p>This file contains only the registration hooks to link our implementations to the generated WIT wrapper: <pre><code>package main\n\nimport (\n    \"gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/bindings/wasm/gen\"\n)\n\nfunc init() {\n    // Register the interface implementations with the generated WIT bindings\n    wasi_polyglot_reference.SetExportsOuvrageLabWasiDemoGeometry(geometryImpl{})\n    wasi_polyglot_reference.SetExportsOuvrageLabWasiDemoStore(storeImpl{})\n    wasi_polyglot_reference.SetExportsOuvrageLabWasiDemoLang(langImpl{})\n    wasi_polyglot_reference.SetExportsOuvrageLabWasiDemoNetwork(networkImpl{})\n}\n\nfunc main() {\n    // Dummy main function required by TinyGo compiler\n}\n</code></pre></p>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"go-and-wit/#b-interface-implementation-example-v2geometrygo","level":3,"title":"B. Interface Implementation (Example: <code>v2/geometry.go</code>)","text":"<p>Each interface is implemented in a sister file belonging to <code>package main</code>. It handles structural conversion and delegates the calculation to the core library package (<code>pkg/geometry</code>): <pre><code>package main\n\nimport (\n    \"gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/bindings/wasm/gen\"\n    \"gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/pkg/geometry\"\n)\n\ntype geometryImpl struct{}\n\nfunc (g geometryImpl) Distance(a, b wasi_polyglot_reference.ExportsOuvrageLabWasiDemoGeometryPoint) float64 {\n    // Convert generated WIT types to internal pkg/geometry domain models\n    gp1 := geometry.Point{X: a.X, Y: a.Y}\n    gp2 := geometry.Point{X: b.X, Y: b.Y}\n    return geometry.Distance(gp1, gp2)\n}\n// ...\n</code></pre></p> <p>This modular architecture ensures that as the library grows, the WASM entrypoint (<code>main.go</code>) remains minimal and the implementation files mirror the domain logic structure.</p>","path":["Go Core &amp; The WIT Contract"],"tags":[]},{"location":"ipc-dilemma/","level":1,"title":"The IPC Dilemma: Stdio vs. In-Process","text":"<p>When integrating compiled WebAssembly modules into scripting languages (like Python or JavaScript), the primary challenge is communication: how does the host language send parameters to the WASM module and retrieve results?</p> <p>This laboratory implements two fundamentally different communication architectures: Stdio JSON-RPC (V1) and In-Process Component Calls (V2). This page explores their design, implementation, and performance trade-offs.</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#1-wasi-preview-1-v1-stdio-json-rpc-bridge","level":2,"title":"1. WASI Preview 1 (V1): Stdio JSON-RPC Bridge","text":"<p>Because WASI Preview 1 restricts the WASM binary to a basic POSIX system model, passing complex data structures directly via memory pointers is highly complex. The standard solution is to treat the WASM binary as a child process and use Inter-Process Communication (IPC).</p> <pre><code>    ┌────────────┐                                ┌────────────┐\n    │            │ ── Stdin (Write JSON-RPC) ──&gt;  │            │\n    │ HOST SHELL │                                │  GO WASM   │\n    │ (JS / Py)  │ &lt;── Stdout (Read JSON-RPC) ─── │  (wasip1)  │\n    │            │                                │            │\n    └────────────┘                                └────────────┘\n</code></pre>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#how-it-works","level":3,"title":"How it works","text":"<ol> <li>Subprocess Spawning: The host runtime (e.g., Python or Node.js) spawns the WASM runtime (<code>wasmtime</code> or <code>node</code> wrapper) as a child process.</li> <li>Request Serialization: The host serializes the function call and arguments into a JSON-RPC structure (e.g., <code>{\"method\": \"geometry.distance\", \"params\": [p1, p2], \"id\": 1}</code>).</li> <li>Transmission: The host writes the JSON string to the child process's Stdin stream.</li> <li>Processing: The Go WASM binary block-reads Stdin, parses the JSON, executes the command, and writes the JSON-RPC response to its Stdout stream.</li> <li>Parsing: The host reads Stdout, deserializes the JSON, and returns the result.</li> </ol>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#advantages","level":3,"title":"Advantages","text":"<ul> <li>Simple Boundaries: Stdin/Stdout streams are supported by 100% of scripting environments.</li> <li>Decoupled Memory: The WASM memory heap is completely isolated from the host process.</li> </ul>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#disadvantages","level":3,"title":"Disadvantages","text":"<ul> <li>High Latency: Spawning a process and executing IPC streams is extremely slow.</li> <li>Serialization Overhead: Parsing and stringifying JSON on every single call consumes substantial CPU cycles.</li> <li>No Stateful Integration: Sharing complex, mutable memory objects (like database connections or cache buffers) across processes is practically impossible.</li> </ul>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#2-wasi-preview-2-v2-in-process-component-calls","level":2,"title":"2. WASI Preview 2 (V2): In-Process Component calls","text":"<p>WASI Preview 2 discards standard streams and process spawning. The WASM module is loaded directly into the host process's memory space (in-process).</p> <pre><code>               IN-PROCESS MEMORY (Single OS Process)\n    ┌────────────────────────────────────────────────────────┐\n    │                                                        │\n    │  ┌────────────┐     Direct Function Call     ┌──────┐  │\n    │  │ HOST SHELL │ ───────────────────────────&gt; │  GO  │  │\n    │  │ (JS / Py)  │ &lt;─────────────────────────── │ WASM │  │\n    │  └────────────┘   Native Types (Memory Map)  └──────┘  │\n    │                                                        │\n    └────────────────────────────────────────────────────────┘\n</code></pre>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#how-it-works_1","level":3,"title":"How it works","text":"<ol> <li>Direct Loading: The host loads the WASM component into its own process thread using native WebAssembly bindings.</li> <li>Type Mapping: The types defined in the WIT contract are mapped directly to native classes (e.g., a JavaScript Class wrapper mapping to a Go struct pointer).</li> <li>Direct Call: When calling <code>geometry.distance(...)</code>, the host directly calls the WASM function exported inside the WebAssembly linear heap.</li> <li>No Serialization: Arguments are passed via memory pointer references. No JSON-RPC or text parsing is involved.</li> </ol>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#advantages_1","level":3,"title":"Advantages","text":"<ul> <li>Near-Zero Latency: Function calls execute in microseconds.</li> <li>Stateful Resources: Allows exporting stateful resources (like an in-memory KV-Store database) that persist in the Go WASM heap between calls.</li> <li>No Child Process: Runs perfectly in browsers and workers where spawning subprocesses is banned.</li> </ul>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#3-real-performance-benchmarks-our-lab-results","level":2,"title":"3. Real Performance Benchmarks (Our Lab Results)","text":"<p>We executed the unit test suites under Node.js for both versions. The suite runs 7 operations (geometry math and string manipulations).</p> <p>Here are the actual execution timings recorded on this machine:</p> Environment V1 (Stdio JSON-RPC Subprocess) V2 (In-Process Component) Performance Gain Node.js Test Suite ~300 ms (total execution) ~103 ms (total execution) ~3x Speedup Single Function Call ~45 ms (average per call) ~0.3 ms (average per call) 150x Speedup","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#why-is-the-v2-function-call-150x-faster","level":3,"title":"Why is the V2 function call 150x faster?","text":"<p>In V1, every function call incurs the cost of writing to standard streams, flushing buffers, context switching in the OS scheduler, parsing JSON inside Go, executing, writing to Stdout, and parsing JSON in JS. In V2, calling <code>geometry.distance</code> is a direct virtual machine jump. It takes less than 300 microseconds (0.3 milliseconds).</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#boilerplate-maintenance-custom-router-vs-native-vm-mapping","level":2,"title":"🛠️ Boilerplate &amp; Maintenance: Custom Router vs. Native VM Mapping","text":"<p>Beyond pure performance, the transition from WASI V1 to WASI V2 represents a massive shift in developer experience and code maintainability:</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#wasi-v1-json-rpc-router-framework","level":3,"title":"WASI V1 (JSON-RPC Router Framework)","text":"<p>To execute code in V1, you are forced to build and maintain a custom routing framework inside the WASM binary.</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#reusable-infrastructure-the-wasi1rpc-tool","level":4,"title":"🛠️ Reusable Infrastructure: The <code>wasi1rpc</code> Tool","text":"<p>Rather than forcing developers to copy-paste low-level stream reading and JSON decoding boilerplates, this repository provides a dedicated, lightweight, and dependency-free helper library: <code>tools/wasi1rpc/</code>.</p> <p>Any Go project running under WASI Preview 1 can import and reuse this framework directly from this public repository.</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#how-to-import-use-wasi1rpc","level":4,"title":"📥 How to Import &amp; Use <code>wasi1rpc</code>","text":"<ol> <li> <p>Add the package dependency to your Go module:     <pre><code>go get gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/tools/wasi1rpc\n</code></pre></p> </li> <li> <p>Bootstrap your main application loop in your <code>package main</code>:     <pre><code>package main\n\nimport (\n    \"gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/tools/wasi1rpc\"\n)\n\nfunc main() {\n    // Initialize the framework application (handles stdin/stdout loops)\n    app := wasi1rpc.NewApp()\n\n    // Register your typed handlers (receives raw json.RawMessage parameters)\n    app.Router.Register(\"geometry.distance\", func(params json.RawMessage) (any, error) {\n        // Decode your custom arguments struct and execute your package methods\n        ...\n    })\n\n    // Start the blocking JSON-RPC listener loop\n    app.Run()\n}\n</code></pre></p> </li> <li> <p>Compile target using the standard Go compiler targeting Preview 1:     <pre><code>GOOS=wasip1 GOARCH=wasm go build -o my_app.wasm ./main.go\n</code></pre></p> </li> <li> <p>The Host Side: You must still maintain a loader script in Python or JavaScript (like <code>loader.py</code>) that wraps <code>wasmtime</code> execution, writes JSON envelopes to stdin, and parses stdout lines.</p> </li> </ol>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#wasi-v2-zero-boilerplate-native-mapping","level":3,"title":"WASI V2 (Zero-Boilerplate Native Mapping)","text":"<p>In WASI V2, this entire routing layer is completely eliminated.  *   No Router Code: Go functions are mapped directly to JavaScript/Python methods via the compiled WIT bindings.  *   Compile-Time Contracts: If the WIT contract matches, the bindings generator (<code>wit-bindgen</code> and <code>jco</code>) handles the underlying memory copies, structure allocations, and function calls automatically. *   The VM is the Router: The WebAssembly System Interface virtual machine acts as the router, resolving type boundaries natively inside the process memory.</p>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"ipc-dilemma/#conclusion","level":2,"title":"Conclusion","text":"<ul> <li>Use V1 only if you are dealing with legacy platforms that do not support WASM Component loaders.</li> <li>Use V2 for all modern applications where performance, browser compatibility, and rich object-oriented architectures are required.</li> </ul>","path":["The IPC Dilemma: Stdio vs. In-Process"],"tags":[]},{"location":"js-host/","level":1,"title":"JavaScript Host Integration &amp; Bundlers","text":"<p>This page details how to execute compiled WebAssembly components inside JavaScript runtimes, covering Node.js native execution, browser native loading (via Import Maps), and configurations for modern bundlers (Vite &amp; Webpack 5).</p>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#1-directory-structure","level":2,"title":"1. Directory Structure","text":"<p>Our JavaScript packages are structured under <code>bindings/js/</code> (and <code>bindings/node/v1/</code> for the legacy node loader):</p> <pre><code>bindings/js/\n  ├── v0/                              # Pure WASM Browser MVP\n  │    ├── index.html                  # Demo container\n  │    └── my_lib/\n  │         ├── index.js               # Export facade\n  │         └── _generated/\n  │              ├── loader.js         # Custom loader with MIME-type fallback\n  │              └── my_lib_v0.wasm    # Stateless Go WASM binary\n  │\n  └── v2/                              # WASI Preview 2 Component\n       ├── package.json                # Declares @bytecodealliance/preview2-shim dependency\n       ├── my_lib/\n       │    ├── index.js               # Entry point\n       │    ├── geometry.js            # Point / Rectangle class wrappers\n       │    ├── store.js               # Stateful KV-Store wrapper\n       │    └── _generated/\n       │         ├── my_lib_component.js # jco transpiled ES Modules\n       │         └── *.wasm            # Go Component binaries\n       │\n       └── samples/\n            ├── esm/                   # Browser integration without bundlers\n            ├── vite/                  # Local Vite project integration\n            └── webpack/               # Webpack 5 custom configuration\n</code></pre>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#2-wasi-preview-2-browser-loading-import-maps","level":2,"title":"2. WASI Preview 2 Browser Loading: Import Maps","text":"<p>Transpiled component ES Modules generated by <code>jco</code> import system shims (e.g., <code>import { environment } from '@bytecodealliance/preview2-shim/cli'</code>). In a pure browser environment, the browser doesn't know where to find <code>@bytecodealliance/preview2-shim</code> because there are no local <code>node_modules</code> folders.</p> <p>To resolve this natively in-browser without any build tools, we define a W3C Standard Import Map:</p> <pre><code>&lt;script type=\"importmap\"&gt;\n{\n  \"imports\": {\n    \"@bytecodealliance/preview2-shim/cli\": \"https://esm.sh/@bytecodealliance/preview2-shim/cli\",\n    \"@bytecodealliance/preview2-shim/clocks\": \"https://esm.sh/@bytecodealliance/preview2-shim/clocks\",\n    \"@bytecodealliance/preview2-shim/filesystem\": \"https://esm.sh/@bytecodealliance/preview2-shim/filesystem\",\n    \"@bytecodealliance/preview2-shim/io\": \"https://esm.sh/@bytecodealliance/preview2-shim/io\",\n    \"@bytecodealliance/preview2-shim/random\": \"https://esm.sh/@bytecodealliance/preview2-shim/random\"\n  }\n}\n&lt;/script&gt;\n\n&lt;script type=\"module\"&gt;\n    // Natively imported! Browser downloads shims from CDN dynamically\n    import { geometry, KVStore } from './my_lib/index.js';\n\n    const db = new KVStore();\n    await db.set(\"user\", \"gpineda\");\n&lt;/script&gt;\n</code></pre>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#3-bundler-configurations","level":2,"title":"3. Bundler Configurations","text":"<p>When compiling web applications using build tools, the bundler must resolve the shims and compile the WebAssembly assets.</p>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#a-vite-integration","level":3,"title":"A. Vite Integration","text":"<p>Vite resolves local module dependencies out-of-the-box using standard Node module resolution. In our Vite sample <code>package.json</code>, we link to our library locally: <pre><code>  \"dependencies\": {\n    \"my-lib-v2\": \"file:../..\"\n  }\n</code></pre> Vite resolves the ES Modules and automatically links the browser-compatible shims during development. No custom config is required.</p>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#b-webpack-5-integration","level":3,"title":"B. Webpack 5 Integration","text":"<p>Webpack 5 requires explicit configuration to handle WASI components correctly. The configuration file <code>webpack.config.js</code> must define these rules:</p> <ol> <li>Enable WebAssembly Experiments:     Async WebAssembly loading is disabled by default in Webpack 5. We must enable it along with outputting the bundle as an ES Module:     <pre><code>  experiments: {\n    outputModule: true,\n    asyncWebAssembly: true\n  }\n</code></pre></li> <li>Mock Node System APIs (Fallback):     Because <code>@bytecodealliance/preview2-shim</code> references Node-specific filesystems and streams that browsers lack, we configure Webpack to ignore these dependencies when building for the browser:     <pre><code>  resolve: {\n    fallback: {\n      fs: false,\n      os: false,\n      path: false,\n      crypto: false,\n      stream: false\n    }\n  }\n</code></pre></li> <li>Bypass Node Scheme Imports (<code>externals</code>):     Webpack fails when encountering import statements using the <code>node:</code> scheme (like <code>import 'node:fs/promises'</code>). We mark these imports as external so Webpack leaves them intact in the output file (they are never executed by Chrome in our runtime calculation path):     <pre><code>  externals: {\n    'node:fs/promises': 'module node:fs/promises',\n    'node:fs': 'module node:fs'\n  }\n</code></pre></li> <li>Inject script as ES Module:     To allow the final bundle to execute modern syntax like <code>import.meta</code> (required to resolve WASM relative paths), the generated HTML file must load the bundle script with <code>type=\"module\"</code>:     <pre><code>  plugins: [\n    new HtmlWebpackPlugin({\n      template: './index.html',\n      scriptLoading: 'module' // Forces &lt;script type=\"module\"&gt;\n    })\n  ]\n</code></pre></li> </ol>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"js-host/#4-automation-commands","level":2,"title":"4. Automation Commands","text":"<p>To run tests and demo servers (Vite and Webpack) using our pre-packaged local Node v22: <pre><code># Run Node.js native unit tests (V1 vs V2)\nmake run-node-v1\nmake run-node-v2\n\n# Start Vite browser dev server\nmake run-vite-sample\n\n# Start Webpack 5 browser dev server\nmake run-webpack-sample\n</code></pre></p>","path":["JavaScript Host Integration &amp; Bundlers"],"tags":[]},{"location":"live-demo/","level":1,"title":"Live WASI V2 Browser Demo","text":"<p>Below is a live, interactive execution of our actual WASI Preview 2 Go Component running directly inside your browser. No server side, no child processes. It instantiates the Go virtual machine in memory, executes rich object-oriented geometry operations, and persists keys inside a stateful Go database.</p>","path":["Live WASI V2 Browser Demo"],"tags":[]},{"location":"live-demo/#interactive-playground","level":2,"title":"⚡ Interactive Playground","text":"Stateful KV-Store (Go Memory Heap) <p>This widget instantiates a new <code>KVStore</code> Go resource. Values are saved inside Go's linear memory heap between calls.</p> Write Key/Value: Set Read Key: Get Awaiting input... OOP Geometry &amp; Lang exports <p>Calls exported Go structs. Values are mapped on-the-fly from JavaScript objects to WASM memory records.</p> Point 1 (X, Y) &amp; Point 2 (X, Y): → Format message for name: Run Tools Awaiting execution... Delegated Host HTTP Client (imported capability) <p>         Go WASM does not have raw socket network access inside the sandbox. When calling <code>network.fetchAndFormat()</code>, Go delegates the request to the host. The browser's JS engine fetches the URL and returns the text, which Go formats and returns.     </p> Fetch URL: Fetch &amp; Format Awaiting fetch request...      Loading WASI V2 Component engine...","path":["Live WASI V2 Browser Demo"],"tags":[]},{"location":"live-demo/#the-implementation-mechanics","level":2,"title":"🛠️ The Implementation Mechanics","text":"<pre><code>sequenceDiagram\n    participant Browser as Host JS Engine (Browser)\n    participant GoWasm as Go WASM Component (heap)\n\n    Browser-&gt;&gt;GoWasm: network.fetchAndFormat(url)\n    Note over GoWasm: Execution enters Go WASM\n    GoWasm-&gt;&gt;Browser: import host-http.fetchUrl(url)\n    Note over Browser: Browser executes AJAX request\n    Browser--&gt;&gt;GoWasm: return text string\n    Note over GoWasm: Go formats response string\n    GoWasm--&gt;&gt;Browser: return formatted string</code></pre>","path":["Live WASI V2 Browser Demo"],"tags":[]},{"location":"live-demo/#browser-compatibility-notes","level":3,"title":"Browser Compatibility Notes","text":"<p>Because the V2 Component loading is fully compliant with modern standards, it runs natively inside the web thread. It uses an Import Map to map WASI system calls to CDN polyfills and loads the transpiled ES Modules using standard async imports:</p> <pre><code>// Dynamic import from the static assets copied inside the documentation\nimport { geometry, KVStore, lang, network } from '../js/v2/my_lib/index.js';\n</code></pre>","path":["Live WASI V2 Browser Demo"],"tags":[]},{"location":"python-host/","level":1,"title":"Python Host Integration","text":"<p>This page details how to load and execute compiled WebAssembly modules inside CPython, comparing the legacy WASI Preview 1 subprocess runner (V1) and the native WASI Preview 2 Component loader (V2).</p>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#1-directory-structure","level":2,"title":"1. Directory Structure","text":"<p>Our Python packages are structured under <code>bindings/py/</code> to demonstrate modular library design:</p> <pre><code>bindings/py/\n  ├── v1/\n  │    ├── pyproject.toml              # Dependencies (pytest, uv)\n  │    └── my_lib/\n  │         ├── index.py               # Public API facade (V1)\n  │         └── _generated/\n  │              ├── loader.py         # Subprocess JSON-RPC executor\n  │              └── my_lib.wasm       # Compiled V1 wasip1 binary\n  │\n  └── v2/\n       ├── pyproject.toml              # Dependencies (pytest, wasmtime, uv)\n       └── my_lib/\n            ├── index.py               # Public API facade wrapping WASM types\n            ├── geometry.py            # Rich Point/Rectangle class wrappers\n            ├── store.py               # Stateful KVStore resource wrapper\n            └── _generated/\n                 └── my_lib_component.wasm # Compiled V2 Component binary\n</code></pre>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#2-wasi-preview-1-v1-subprocess-json-rpc","level":2,"title":"2. WASI Preview 1 (V1) - Subprocess JSON-RPC","text":"<p>In V1, Python executes the WASM binary by spawning a subprocess running the compiled binary under the <code>wasmtime</code> CLI. </p> <p>The loader <code>my_lib/_generated/loader.py</code> handles the interaction: <pre><code>import subprocess\nimport json\n\ndef call_wasm(method: str, params: list):\n    # Spawn wasmtime running the wasip1 binary\n    proc = subprocess.Popen(\n        [\"wasmtime\", \"my_lib.wasm\"],\n        stdin=subprocess.PIPE,\n        stdout=subprocess.PIPE,\n        stderr=subprocess.PIPE,\n        text=True\n    )\n    # Write JSON-RPC payload to stdin\n    payload = json.dumps({\"method\": method, \"params\": params})\n    stdout, stderr = proc.communicate(input=payload)\n\n    # Parse output response\n    response = json.loads(stdout)\n    return response[\"result\"]\n</code></pre> Disadvantage: Every call spawns a process, parsing strings, making it extremely slow.</p>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#3-wasi-preview-2-v2-in-process-wasmtime-loader","level":2,"title":"3. WASI Preview 2 (V2) - In-Process Wasmtime Loader","text":"<p>In V2, Python loads the component directly into the python process thread using the <code>wasmtime</code> library. </p>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#a-dynamic-component-loading","level":3,"title":"A. Dynamic Component Loading","text":"<p>The loader <code>bindings/py/v2/my_lib/_generated/loader.py</code> boots the WASM engine in memory: <pre><code>from wasmtime import Engine, Store, Linker, Component\n\nengine = Engine()\nlinker = Linker(engine)\n\n# Load the compiled component\ncomponent = Component.from_file(engine, \"my_lib_component.wasm\")\nstore = Store(engine)\n\n# Instantiate the component inside Python's process\ninstance = linker.instantiate(store, component)\n</code></pre></p>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#b-mapping-wit-types-to-python-oop-class-wrappers","level":3,"title":"B. Mapping WIT Types to Python OOP Class wrappers","text":"<p>Because <code>wasmtime</code> translates WIT records and resources to raw dictionary entries and index pointers, our public facade wraps them into clean Python classes to preserve static type checking and auto-completion.</p> <p>For example, <code>my_lib/geometry.py</code> implements Point geometry: <pre><code>from my_lib._generated import loader\n\nclass Point:\n    def __init__(self, x: float, y: float):\n        self.x = x\n        self.y = y\n\n    async def distance_to(self, other: 'Point') -&gt; float:\n        # Calls the in-memory WASM function directly\n        return loader.geometry.distance(\n            loader.store, \n            {\"x\": self.x, \"y\": self.y}, \n            {\"x\": other.x, \"y\": other.y}\n        )\n</code></pre></p> <p>And <code>my_lib/store.py</code> wraps the stateful KV-Store resource: <pre><code>from my_lib._generated import loader\n\nclass KVStore:\n    def __init__(self):\n        # Instantiate a new stateful KVStore resource in Go heap\n        self._handle = loader.store_exports.KvStore(loader.store)\n\n    async def set(self, key: str, value: str) -&gt; None:\n        self._handle.set(loader.store, key, value)\n\n    async def get(self, key: str) -&gt; str:\n        return self._handle.get(loader.store, key)\n</code></pre></p>","path":["Python Host Integration"],"tags":[]},{"location":"python-host/#4-execution-testing","level":2,"title":"4. Execution &amp; Testing","text":"<p>We manage dependencies and virtual environments in python using <code>uv</code> (compiled Rust-based package manager).</p> <p>To execute the unit tests for Python V1 (JSON-RPC) or V2 (Component): <pre><code># Run Preview 1 tests\nmake run-py-v1\n\n# Run Preview 2 tests\nmake run-py-v2\n</code></pre> The V2 component tests run in ~0.13 seconds compared to ~0.54 seconds for the V1 subprocess suite.</p>","path":["Python Host Integration"],"tags":[]},{"location":"toolchain-guide/","level":1,"title":"Toolchain &amp; Compilation Workflow","text":"<p>Compiling high-level Go code into WASI Preview 2 Components involves a multi-step compilation toolchain. This guide details the toolchain prerequisites, the compilation steps, and the automation configured in our <code>Makefile</code>.</p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#1-toolchain-prerequisites","level":2,"title":"1. Toolchain Prerequisites","text":"<p>Because WASI Preview 2 is a modern ecosystem, standard compilation toolchains require specific versions. To prevent system-level dependency conflicts, our setup installs toolchain binaries locally inside the <code>bin/</code> folder of this repository:</p> <ul> <li>TinyGo (v0.31.2): Used for compiling Go code. TinyGo is required instead of standard Go because it produces much smaller WASM binaries and natively supports compiling reactor modules without garbage collection overhead.</li> <li>wit-bindgen (v0.24.0): Generates the low-level translation layers in Go, Python, and JS from the WIT interface description.</li> <li>wasm-tools (v1.200.0): Used to embed WIT metadata into the compiled WASM binary and translate the WASI Preview 1 binary into a WASI Preview 2 Component.</li> <li>Local Node.js (v22.12.0): Bundled in <code>bin/node/</code> to execute <code>jco</code> transpilation steps and run unit tests.</li> </ul> <p>To boot the environment and download all tools locally, run: <pre><code>make setup\n</code></pre></p> <p>Under the hood, this Makefile target delegates to the centralized bootstrap script <code>tools/bootstrap-wasi.sh</code>.</p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#granular-unit-toolchain-installation","level":3,"title":"📥 Granular &amp; Unit Toolchain Installation","text":"<p>To allow other repositories (like <code>ouvrage-kern-go</code>) to reuse this toolchain setup without pulling in heavy, unnecessary runtimes like Node.js, the bootstrap script supports granular installation flags:</p> <ul> <li><code>--dir &lt;path&gt;</code>: Specifies the output directory for binaries (defaults to <code>./bin</code>).</li> <li><code>--tinygo</code>: Installs only TinyGo.</li> <li><code>--wit-bindgen</code>: Installs only the <code>wit-bindgen</code> CLI tool.</li> <li><code>--wasm-tools</code>: Installs only the <code>wasm-tools</code> CLI tool.</li> <li><code>--adapter</code>: Installs only the WASI <code>wasi_snapshot_preview1.wasm</code> reactor adapter.</li> <li><code>--node</code>: Installs only the Node.js runtime environment.</li> <li><code>--all</code> (default): Installs all five components.</li> </ul> <p>For example, a purely Go-focused project can bootstrap its WASM compiler setup instantly using a single curl command while skipping Node.js: <pre><code>curl -sSfL https://gitlab.com/ouvrage-systems/labs/wasi-polyglot-bindings-reference/-/raw/main/tools/bootstrap-wasi.sh \\\n  | bash -s -- --dir ./bin --tinygo --wit-bindgen --wasm-tools --adapter\n</code></pre></p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#2-compilation-workflows","level":2,"title":"2. Compilation Workflows","text":"<p>Our <code>Makefile</code> automates three separate targets representing the evolution of WebAssembly:</p> <pre><code>  [v0: Pure WASM]  ──&gt; TinyGo (-target=wasm) ──────────────────────────&gt; my_lib_v0.wasm\n\n  [v1: wasip1]     ──&gt; Go Compiler (GOOS=wasip1 GOARCH=wasm) ─────────&gt; my_lib.wasm\n\n  [v2: wasip2]     ──&gt; wit-bindgen ──&gt; TinyGo ──&gt; wasm-tools ──&gt; jco ──&gt; ESM Component\n</code></pre>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#a-v0-compilation-pure-browser-wasm","level":3,"title":"A. V0 Compilation (Pure Browser WASM)","text":"<p>Compiles a stateless arithmetic Go package to browser-compatible WebAssembly MVP. <pre><code>tinygo build -o bindings/js/v0/my_lib/_generated/my_lib_v0.wasm -target=wasm ./bindings/wasm/v0\n</code></pre></p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#b-v1-compilation-posix-wasi-preview-1","level":3,"title":"B. V1 Compilation (POSIX WASI Preview 1)","text":"<p>Compiles the JSON-RPC stdin/stdout subprocess runner. <pre><code>GOOS=wasip1 GOARCH=wasm go build -o bindings/py/v1/my_lib/_generated/my_lib.wasm ./bindings/wasm/v1\n</code></pre></p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#c-v2-compilation-component-model-wasi-preview-2","level":3,"title":"C. V2 Compilation (Component Model WASI Preview 2)","text":"<p>This is the most complex pipeline, consisting of 5 steps automated by <code>make build-wasm-v2</code>:</p> <pre><code>graph TD\n    classDef file fill:#ECEFF1,stroke:#37474F,stroke-width:1px,rx:5px,ry:5px;\n    classDef tool fill:#E8EAF6,stroke:#1A237E,stroke-width:1px,rx:5px,ry:5px;\n\n    WIT[\"📂 bindings/wit/&lt;br&gt;(WIT spec files)\"]:::file\n    GO_SRC[\"📂 bindings/wasm/v2/&lt;br&gt;(Go core implementation)\"]:::file\n    GEN_GO[\"📂 bindings/wasm/gen/&lt;br&gt;(Generated Go bindings)\"]:::file\n\n    WIT --&gt;|1. wit-bindgen| GEN_GO\n\n    GO_SRC --&gt;|2. tinygo build| RAW_WASM[\"📄 bindings/wasm/my_lib_raw.wasm&lt;br&gt;(WASI Preview 1 reactor)\"]:::file\n    GEN_GO --&gt;|2. tinygo build| RAW_WASM\n\n    WIT --&gt;|3. wasm-tools embed| EMBED_WASM[\"📄 bindings/wasm/my_lib_embedded.wasm&lt;br&gt;(WASM with WIT metadata)\"]:::file\n    RAW_WASM --&gt;|3. wasm-tools embed| EMBED_WASM\n\n    ADAPTER[\"📄 bin/wasi_snapshot_preview1.wasm&lt;br&gt;(WASI P1-to-P2 Adapter)\"]:::file\n    EMBED_WASM --&gt;|4. wasm-tools new| COMP_WASM[\"📄 my_lib_component.wasm&lt;br&gt;(WASI Preview 2 Component)\"]:::file\n    ADAPTER --&gt;|4. wasm-tools new| COMP_WASM\n\n    COMP_WASM --&gt;|5. jco transpile| JS_ESM[\"📂 my_lib/_generated/&lt;br&gt;(ES Modules &amp; TS Declarations)\"]:::file</code></pre> <ol> <li>Generate Go WIT Bindings:     Runs <code>wit-bindgen</code> to parse the WIT specs directory <code>bindings/wit</code> and produce Go bindings inside <code>bindings/wasm/gen/</code>.     <pre><code>wit-bindgen tiny-go ./bindings/wit --out-dir ./bindings/wasm/gen\n</code></pre></li> <li>Compile Go WASI Reactor:     TinyGo compiles the Go module into a WASI Preview 1 reactor binary (<code>my_lib_raw.wasm</code>). The target is <code>wasi</code> and the build uses <code>-no-debug</code> to reduce size.     <pre><code>tinygo build -o ./bindings/wasm/my_lib_raw.wasm -target=wasi -no-debug ./bindings/wasm/v2\n</code></pre></li> <li>Embed WIT Metadata:     Runs <code>wasm-tools component embed</code> to inject the compiled WIT metadata directly into the WASM binary.     <pre><code>wasm-tools component embed ./bindings/wit ./bindings/wasm/my_lib_raw.wasm -o ./bindings/wasm/my_lib_raw.wasm\n</code></pre></li> <li>Translate to V2 Component (The Adapter):     Runs <code>wasm-tools component new</code> to translate the Preview 1 system calls (<code>wasip1</code>) into Preview 2 interface imports (<code>wasip2</code>). This step requires the adapter file <code>wasi_snapshot_preview1.wasm</code> which acts as a bridge.     <pre><code>wasm-tools component new ./bindings/wasm/my_lib_raw.wasm -o ./bindings/py/v2/my_lib/_generated/my_lib_component.wasm --adapt ./bin/wasi_snapshot_preview1.wasm\n</code></pre></li> <li>Transpile to ES Modules (for JavaScript Hosts):     Runs <code>jco transpile</code> on the generated component to produce standard browser-compatible JavaScript ES Modules and TypeScript <code>.d.ts</code> declaration files.     <pre><code>npx @bytecodealliance/jco transpile ./bindings/py/v2/my_lib/_generated/my_lib_component.wasm -o ./bindings/js/v2/my_lib/_generated\n</code></pre></li> </ol>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#3-customizing-the-contract","level":2,"title":"3. Customizing the Contract","text":"<p>If you need to extend or modify the library capabilities, you will update the interface contract files. Here is the step-by-step developer workflow to propagate changes through the decoupled architecture:</p>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#step-1-update-the-wit-specification-v2-only","level":3,"title":"Step 1: Update the WIT Specification (V2 Only)","text":"<ol> <li>Modify or add <code>.wit</code> specification files inside <code>bindings/wit/</code>.</li> <li>Regenerate the Go type definitions by running:     <pre><code>make build-wasm-v2\n</code></pre> (Note: This step will temporarily fail at Step 2 of compilation because the Go code does not yet implement the new interface).</li> </ol>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#step-2-update-the-go-implementations","level":3,"title":"Step 2: Update the Go Implementations","text":"<ul> <li> <p>For WASI V2 (Component Model):</p> <ol> <li>Update the specific module file inside <code>bindings/wasm/v2/</code> (<code>geometry.go</code>, <code>store.go</code>, <code>lang.go</code>, or <code>network.go</code>) to match the new generated signatures from <code>bindings/wasm/gen/</code>.</li> <li>If you added a completely new interface, register its implementation in the <code>init()</code> block of <code>bindings/wasm/v2/main.go</code>.</li> </ol> </li> <li> <p>For WASI V1 (JSON-RPC):</p> <ol> <li>Update the JSON-RPC handler callbacks inside <code>bindings/wasm/v1/</code> (<code>geometry.go</code>, <code>store.go</code>, or <code>lang.go</code>) to handle the new method string and unpack/pack arguments.</li> <li>If you added a new handler, register it in <code>bindings/wasm/v1/main.go</code>.</li> </ol> </li> </ul>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"toolchain-guide/#step-3-recompile-and-verify","level":3,"title":"Step 3: Recompile and Verify","text":"<ol> <li>Re-run the build to compile all targets and transpile JS modules:     <pre><code>make build\n</code></pre></li> <li>Verify your changes by running the test suites:     <pre><code>make test\n</code></pre></li> </ol>","path":["Toolchain &amp; Compilation Workflow"],"tags":[]},{"location":"troubleshooting/","level":1,"title":"Developer Tips &amp; Troubleshooting","text":"<p>This page documents engineering tricks, workarounds, and common solutions compiled during the integration of WASI Preview 2 components into polyglot host scripting environments.</p>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#1-local-server-wasm-mime-type-constraints","level":2,"title":"1. Local Server WASM MIME-Type Constraints","text":"","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-problem","level":3,"title":"The Problem","text":"<p>When testing web pages containing WebAssembly locally using simple servers (like VS Code Live Server or Python's default <code>http.server</code>), you may encounter the following browser console error: <pre><code>Uncaught (in promise) TypeError: Failed to execute 'compile' on 'WebAssembly': \nIncorrect response MIME type. Expected 'application/wasm', got 'text/plain'\n</code></pre> This happens because the W3C spec forces <code>WebAssembly.instantiateStreaming</code> to validate that the HTTP header <code>Content-Type</code> is exactly <code>application/wasm</code>. If the server sends <code>text/plain</code> or <code>application/octet-stream</code>, compilation is blocked.</p>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-solution-arraybuffer-fallback","level":3,"title":"The Solution: ArrayBuffer Fallback","text":"<p>To ensure standalone, bulletproof loading across misconfigured servers, write a fallback loader that falls back to <code>ArrayBuffer</code> compilation (which bypasses MIME-type validation):</p> <pre><code>// Resolve WASM URL relative to the current loader script\nconst wasmUrl = new URL('my_lib.wasm', import.meta.url);\n\n// 1. Attempt fastest standard-compliant streaming compilation\nWebAssembly.instantiateStreaming(fetch(wasmUrl), importObject)\n  .catch((err) =&gt; {\n    console.warn(\"instantiateStreaming failed, falling back to ArrayBuffer:\", err);\n\n    // 2. Fall back to manual ArrayBuffer download, which skips MIME checks\n    return fetch(wasmUrl)\n      .then(response =&gt; response.arrayBuffer())\n      .then(bytes =&gt; WebAssembly.instantiate(bytes, importObject));\n  })\n  .then(({ instance }) =&gt; {\n    // Bind memory and exports\n    const wasmMemory = instance.exports.memory;\n    const exports = instance.exports;\n  });\n</code></pre>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#2-tinygo-standalone-runtime-system-hook-requirements","level":2,"title":"2. TinyGo Standalone Runtime System Hook Requirements","text":"","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-problem_1","level":3,"title":"The Problem","text":"<p>Even when compiling Go code with TinyGo targeting pure WebAssembly (<code>-target=wasm</code>) without any filesystem or system calls, the generated WASM binary will still require imports from a module named <code>wasi_snapshot_preview1</code>. It specifically requires:</p> <ul> <li><code>random_get</code>: Used to feed entropy to seed key hash calculations for standard Go <code>maps</code>.</li> <li><code>fd_write</code>: Used to print runtime call stack logs in the event of a panic.</li> <li><code>proc_exit</code>: Used to stop the execution thread if a fatal panic occurs.</li> </ul> <p>If you instantiate the binary in Chrome without passing these imports, loading throws a missing imports error.</p>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-solution-minimal-browser-mock","level":3,"title":"The Solution: Minimal Browser Mock","text":"<p>You do not need to download heavy WASI shim libraries to run simple modules. Pass a minimal mock environment object directly in the JavaScript imports structure:</p> <pre><code>let wasmMemory;\n\nconst mockWasiEnv = {\n  proc_exit: (code) =&gt; {\n    console.warn(`WASM Go called exit with code: ${code}`);\n  },\n  fd_write: (fd, iovs, iovs_len, nwritten) =&gt; {\n    return 0; // Return success (silence prints)\n  },\n  random_get: (buf, buf_len) =&gt; {\n    // Fill Go's memory buffer with native browser Web Crypto entropy\n    if (wasmMemory) {\n      const array = new Uint8Array(wasmMemory.buffer, buf, buf_len);\n      crypto.getRandomValues(array);\n    }\n    return 0; // Return success\n  }\n};\n\nconst importObject = {\n  wasi_snapshot_preview1: mockWasiEnv\n};\n\nconst { instance } = await WebAssembly.instantiate(wasmBytes, importObject);\nwasmMemory = instance.exports.memory;\n</code></pre>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#3-webpack-5-out-of-the-box-resolutions","level":2,"title":"3. Webpack 5 Out-of-the-box Resolutions","text":"<p>If you are using Webpack 5 to compile your Web application incorporating WASI components, Webpack will throw errors because <code>@bytecodealliance/preview2-shim</code> references Node APIs.</p> <p>Apply these configurations in <code>webpack.config.js</code>: *   Prevent bundling Node APIs: Tell Webpack to ignore system modules inside browser target bundles by configuring <code>resolve.fallback</code>:     <pre><code>resolve: {\n  fallback: {\n    fs: false,\n    os: false,\n    path: false,\n    crypto: false,\n    stream: false\n  }\n}\n</code></pre> *   Externalize Node Scheme Imports: Prevent Webpack from compiling <code>node:</code> scheme prefixes by declaring them as external imports:     <pre><code>externals: {\n  'node:fs/promises': 'module node:fs/promises',\n  'node:fs': 'module node:fs'\n}\n</code></pre></p>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#4-wasi-resource-memory-leaks","level":2,"title":"4. WASI Resource Memory Leaks","text":"","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-problem_2","level":3,"title":"The Problem","text":"<p>When you instantiate a <code>resource</code> in WIT (like our <code>KVStore</code> database), the actual stateful object is instantiated inside the Go WebAssembly linear memory heap. </p> <p>The Python or JavaScript garbage collector has no direct visibility into the WASM heap. If the host GC cleans up the JS/Python wrapper, the corresponding Go database struct inside WASM may remain allocated, causing a memory leak inside the WebAssembly instance.</p>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"troubleshooting/#the-solution-proper-destructuring","level":3,"title":"The Solution: Proper Destructuring","text":"<ul> <li>In JavaScript (<code>jco</code>): <code>jco</code> automatically links JS lifecycle checks to trigger WASM destructors when a resource object is garbage collected. However, to immediately release resources, call the destructor explicitly if supported by the binding wrapper.</li> <li>In Go: The Go Component bindings registry holds resource references in a global map. When a destructor call is received from the host, ensure the Go runtime deletes the reference from its state registry to let the Go GC free the memory.</li> </ul>","path":["Developer Tips &amp; Troubleshooting"],"tags":[]},{"location":"wasi-networking/","level":1,"title":"WASI Sockets &amp; Outbound HTTP Networking","text":"<p>[!NOTE] While this reference laboratory focuses on Geometry calculations and Stateful Key-Value memory execution, this page serves as an architectural guide on how WASI Preview 2 manages networking and outbound HTTP communication in Go.</p> <p>This page explores the networking capabilities of WebAssembly under the WASI Preview 2 paradigm, explaining capabilities-based socket connections and outbound HTTP client requests.</p>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#1-the-sandbox-security-model-capabilities-based-sockets","level":2,"title":"1. The Sandbox Security Model: Capabilities-Based Sockets","text":"<p>In a traditional operating system, any binary can attempt to bind a port or open a socket to any IP address. Security is managed globally (firewalls, user permissions).</p> <p>In WASI Preview 2, WebAssembly enforces a capabilities-based security model:</p> <ul> <li>A WASM component has no ambient authority to make network connections. It cannot call native Go/Rust socket APIs (<code>net.Dial</code> or standard sockets) directly.</li> <li>The component must explicitly import network capabilities in its WIT contract (e.g., <code>wasi:sockets/tcp</code> or <code>wasi:http/outgoing-handler</code>).</li> <li>The host runtime (e.g., Wasmtime) must grant permissions when instantiating the module, explicitly listing which hosts or ports the component is allowed to access.</li> <li>Attempts to connect to unauthorized hosts will throw immediate, sandboxed network errors inside WASM, without reaching the host's OS socket layers.</li> </ul>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#2-low-level-sockets-vs-high-level-protocols","level":2,"title":"2. Low-Level Sockets vs. High-Level Protocols","text":"<p>WASI Preview 2 splits networking into two standardized worlds:</p>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#a-wasi-sockets-wasisockets","level":3,"title":"A. WASI Sockets (<code>wasi:sockets</code>)","text":"<p>For low-level network interactions, WASI provides raw TCP and UDP socket capabilities (defined in <code>wasi:sockets/tcp</code> and <code>wasi:sockets/udp</code>).</p> <ul> <li>Use Case: Implementing custom binary protocols, database clients (PostgreSQL, Redis), or raw socket streaming.</li> <li>Mechanism: The component imports TCP sockets and calls <code>connect</code> or <code>listen</code>. This is mapped by the host runtime to native OS sockets.</li> </ul>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#b-wasi-http-wasihttp","level":3,"title":"B. WASI HTTP (<code>wasi:http</code>)","text":"<p>For standard web-based API clients, WASI defines a high-level HTTP client interface (<code>wasi:http/outgoing-handler</code>).</p> <ul> <li>Use Case: Querying microservices, consuming REST APIs, or interacting with SaaS platforms.</li> <li>Motivation: Standard outbound HTTP allows host runtimes (like Cloudflare Workers or Web Browsers) to optimize requests. In the browser, <code>wasi:http</code> translates directly to native <code>fetch()</code> calls, keeping the execution completely secure and non-blocking.</li> </ul>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#3-go-outbound-http-client-integration","level":2,"title":"3. Go Outbound HTTP Client Integration","text":"<p>When compiling Go code to a WASI Component, the standard Go <code>net/http</code> client does not work natively out-of-the-box because Go's standard library tries to call low-level POSIX sockets that the WASM sandbox blocks.</p> <p>To make outbound HTTP calls in Go inside a WASI component, we use the <code>wasi-http</code> bindings.</p> <p>Here is how you make a request in Go using the standard Preview 2 interfaces:</p> <pre><code>package main\n\nimport (\n    \"fmt\"\n    \"io\"\n    \"net/http\"\n\n    // Import the standard preview2 WASI HTTP round tripper wrapper\n    \"github.com/stealthrocket/wasi-go/imports/wasi/http/client\"\n)\n\nfunc FetchData() (string, error) {\n    // Configure http.Client to use WASI Outgoing Handler instead of POSIX sockets\n    c := &amp;http.Client{\n        Transport: &amp;client.RoundTripper{},\n    }\n\n    resp, err := c.Get(\"https://api.example.com/data\")\n    if err != nil {\n        return \"\", fmt.Errorf(\"HTTP request failed: %w\", err)\n    }\n    defer resp.Body.Close()\n\n    body, err := io.ReadAll(resp.Body)\n    if err != nil {\n        return \"\", fmt.Errorf(\"failed to read response: %w\", err)\n    }\n\n    return string(body), nil\n}\n</code></pre>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]},{"location":"wasi-networking/#4-host-permission-configuration-wasmtime-example","level":2,"title":"4. Host Permission Configuration (Wasmtime Example)","text":"<p>To execute the compiled component with network access, the host launcher must grant explicit permissions. </p> <p>For the Wasmtime CLI, we pass the network configuration flags: <pre><code># Grant access to call outbound HTTP endpoints on port 443\nwasmtime run --wasi-modules=experimental-wasi-http \\\n             --tcplisten=127.0.0.1:0 \\\n             my_component.wasm\n</code></pre></p> <p>This strict decoupling ensures that even if the WASM binary contains malicious code, it cannot execute arbitrary port scans or connect to unauthorized external servers, resolving a major security concern of serverless architectures.</p>","path":["WASI Sockets &amp; Outbound HTTP Networking"],"tags":[]}]}