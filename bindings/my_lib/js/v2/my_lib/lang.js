import { lang } from './_generated/my_lib_component.js';

export async function formatMessage(name) {
  return lang.formatMessage(name);
}

export async function reverseString(s) {
  return lang.reverseString(s);
}
