[project]
name = "my-lib-v1"
version = "0.1.0"
description = "Generic WebAssembly WASI Preview 1 (JSON-RPC) bindings demonstration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "wasmtime>=12.0.0",
    "pytest>=7.0.0",
]

[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"
