[project]
name = "wasi-polyglot-bindings-reference"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.52",
]

[tool.uv.workspace]
members = [
    "bindings/py/all",
]
